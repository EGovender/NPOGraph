---
import BaseLayout from '../layouts/BaseLayout.astro';
import SearchBox from '../components/SearchBox';
import { concepts, relationships, ontologyVersion, ontologyLastUpdated } from '../data/ontology';

const base = import.meta.env.BASE_URL;

const lastUpdatedDisplay = ontologyLastUpdated
  ? new Date(ontologyLastUpdated).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })
  : null;
---

<BaseLayout title="Home">
  <section style="text-align: center; margin-bottom: 2rem;">
    <h1 style="margin-bottom: 0.5rem;">A shared language for grantmaking and nonprofit operations</h1>
    <p class="secondary" style="font-size: 1.15rem; max-width: 60ch; margin: 0 auto;">
      Every funder, grantee, and vendor models a "grant" a little differently. NPOGraph is an open,
      precisely-defined ontology so the same concepts — application, award, payment, compliance,
      outcome — mean the same thing everywhere.
    </p>
    <p class="muted" style="max-width: 55ch; margin: 0.75rem auto 0;">
      Built for grantmaking practitioners, nonprofit-technology teams, and the vendors and
      researchers who support them.
    </p>
  </section>

  <nav class="home-cta-row" aria-label="Primary">
    <a class="home-cta home-cta-primary" href={`${base}explore`}>Explore the graph</a>
    <a class="home-cta" href={`${base}story`}>Follow a worked example</a>
    <a class="home-cta" href={`${base}design`}>Model your operating model</a>
    <a class="home-cta" href={`${base}concepts`}>Browse concepts</a>
  </nav>

  <SearchBox client:load concepts={concepts} base={base} />

  <p class="muted" style="text-align: center; margin-top: 0.75rem;">
    Pre-alpha · Ontology v{ontologyVersion}
    {lastUpdatedDisplay && <>, updated {lastUpdatedDisplay}</>}
    {' '}· {concepts.length} concepts · {relationships.length} relationships
  </p>

  <section style="margin-top: 3rem;">
    <h2>What is this?</h2>
    <p>
      NPOGraph models how grantmaking actually works — funders, applications, reviews, awards,
      payments, compliance, and outcomes, plus the organizational roles and arrangements
      (fiscal sponsorship, donor-advised funds, regranting) that surround them — as a shared,
      openly reviewable ontology. This site is the interactive explorer for that ontology.
    </p>
    <p>
      Grantmaking is the first module; the goal is a broader ontology for nonprofit operations. See
      the project's{' '}
      <a href="https://github.com/EGovender/NPOGraph/blob/main/docs/00-vision.md">vision</a>{' '}
      and <a href="https://github.com/EGovender/NPOGraph/blob/main/docs/04-roadmap.md">roadmap</a>{' '}
      for where it's headed, or the{' '}
      <a href="https://github.com/EGovender/NPOGraph">NPOGraph repository</a> for the underlying
      concepts, relationships, and machine-readable formats (OWL, RDF, JSON-LD).
    </p>
  </section>
</BaseLayout>
